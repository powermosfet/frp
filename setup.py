setup(
        name='frp',
        version='0.1.0',
        install_requires=[],  # Don't put anything here, just use requirements.txt
        packages=['frp'],
        package_dir={'frp': 'frp'},
        )
